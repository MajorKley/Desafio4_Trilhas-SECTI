{
  "build": {
    "env": {
      "API_URL": "@api_url",
      "GOOGLE_MAPS_API_KEY": "@google_maps_key"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}